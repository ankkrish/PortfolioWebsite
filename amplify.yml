version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Skipping npm install for static site"
    build:
      commands:
        - echo "Skipping build for static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
